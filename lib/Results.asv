
def_name = "Results_" + string(datetime("now"));
% filename__ = inputdlg('Choose output file name:', '', [1, 60], def_name);
% filename = string(filename__);

filename = "hello";
fileID = fopen(fullfile('..', 'output',  char() , 'w'); % Creates 'myfile.txt' if it doesn't exist
fprintf(fileID, 'Hello, MATLAB!\n'); % Write text
fclose(fileID); % Close the file





Electrical_Parameters = [];
Electrical_Units = [];

Mechanical_Parameters = [];
Mechanical_Units = [];